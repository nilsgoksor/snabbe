const devMode = false;
export const ROUNDS = !devMode ? "rounds" : "mock_rounds";
export const PLAYERS = !devMode ? "players" : "mock_players";
export const HISTORY = !devMode ? "history" : "mock_histort";
